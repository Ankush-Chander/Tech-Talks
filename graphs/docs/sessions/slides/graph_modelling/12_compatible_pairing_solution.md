# Formulation

Bipartite graph where left side = men, right side = women.  
Edge m–w exists if m and w are compatible (neither dislikes the other).  
Answer = whether a perfect matching exists (use Hopcroft–Karp or maximum flow).
