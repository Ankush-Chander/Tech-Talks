# Formulation

A Hamiltonian path (visiting every vertex exactly once) exists in a tree **iff** the tree is a simple path graph (all nodes have degree ≤ 2).  
In that case, there are exactly 2 valid routes (the two directions). Otherwise, answer = 0.  
