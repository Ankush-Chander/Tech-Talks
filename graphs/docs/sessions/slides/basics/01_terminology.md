# Graph Terminology

![](../../../../images/basics/graph_example.png)  

A graph consists of:

* **Nodes (Vertices)** — the entities in the graph
* **Edges** — connections between nodes

Notation commonly used:

* `n` = number of nodes
* `m` = number of edges
