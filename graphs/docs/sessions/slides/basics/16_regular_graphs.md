# Regular Graphs

![](../../../../images/basics/weighted_graph.png)  

A graph is **regular** if every node has the same degree `d`.

Example:

```text
Cycle graph:

1 — 2
|   |
4 — 3
```

Each node has degree `2`.

Hence it is a **2-regular graph**.
