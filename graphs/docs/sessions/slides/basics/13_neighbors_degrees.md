# Neighbors and Degrees

![](../../../../images/basics/neighbours_degree.png)  

Two nodes are **neighbors** (or **adjacent**) if an edge exists between them.

**The degree** of a node is the number of its neighbors.

Example:

```text
Neighbors of node `2`: {1, 4, 5}
Degree of node `2`: 3
```
