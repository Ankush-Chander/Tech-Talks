# Trees

![](../../../../images/basics/tree.png)  

A **tree** is a connected graph with:

- `n` nodes
- `n − 1` edges

Properties:

1. The graph is connected.
2. There are no cycles.
3. There is exactly one path between any two nodes.
