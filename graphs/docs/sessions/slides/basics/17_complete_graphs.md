# Complete Graphs

![](../../../../images/basics/weighted_graph.png)  

A graph is **complete** if every pair of nodes is connected.

For `n` nodes:

```text
deg(v) = n − 1
```

for every node.

Notation:

```text
Kₙ
```

Example:

```text
K₄
```

Every node is connected to all other nodes.
